declare export class InputStream<-T> {
  write(chunk: T|T[], size?: number): number;
}

let a: InputStream<Animal> = new InputStream<Animal>();
a = new InputStream<Tiger>

declare export class OutputStream<+T> {
  read(size?: number): Promise<T[]>;
}
